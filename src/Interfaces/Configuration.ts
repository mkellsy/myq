export interface Configuration {
    client_id: string;
    client_secret: string;
    redirect_uri: string;
    api_scope: string;
    app_id: string;
    user_agent: string;
    auth_user_agent: string;
    app_version: string;
}
